$$
\bigstar\;\diamond\;\bigstar\quad\boxed{\sigma_{\omega}\sigma}\quad\bigstar\;\diamond\;\bigstar
$$

# 图 4.5

## 理论

极大极小设计相较于聚类设计与极小极大设计的一大优势在于，其能够便捷地适配序贯构造流程。举例来说，我们可以先选取一个样本点 $\boldsymbol{x}_1$，随后按照下述公式依次新增 $\boldsymbol{x}_2,\dots,\boldsymbol{x}_n$：

$$
\boldsymbol{x}_{k+1}=\argmax_{\boldsymbol{x}\in X}\;\min_{i=1:k}\|\boldsymbol{x}-\boldsymbol{x}_i\|,\quad k=1,\dots,n-1 \tag{4.20}
$$

需要注意的是，上述方法属于初始设计的序贯构造方法，而第七章将要介绍的序贯设计方法，是在完整获取所有包含响应值在内的数据之后再开展设计工作。

图 4.5 左图展示了依据最大最小准则，向中心点依次新增六个样本点的过程。该过程可借助 SFDesign 软件包中的 maximin.augment 函数实现。最终生成设计的最小间距 $\psi(D)=0.25$，小于图 4.4 中最优最大最小设计的最小间距。在此设计基础上继续依次追加六个样本点，即可得到图 4.5 右侧图形对应的设计方案，该方案表现优良，但并非包含 13 个样本点的最优最大最小设计。

## 实践

创建画布宽 20 英寸、高 10 英寸，将画布分为 1x2。令 p=2，n=7，设置种子为 1。导入 SFDesign 包，调用 full.factorial() 函数生成 2 维、每一维划分 5 层的全因子网格候选集，其中 5 层即把 $[0,1]$ 分为 $[0,0.25,0.5,0.75,1]$，2 维共 25 个候选点，点集命名为 CAND。调用 maxmin.augment() 函数序贯构造极大极小点，其中点数共 7 个，2 维，初始矩阵为 (0.5,0.5) 的 1x1 矩阵，候选集为 CAND，返回的设计点横纵坐标矩阵命名为 D。依据公式

$$
\psi(D) := \frac{1}{2}\min_{i\neq j}\|\boldsymbol{x}_i-\boldsymbol{x}_j\|
$$

计算 $\psi(D)$ 并打印。绘制 D 的散点图，其中 x 轴标注为 expression(x[1])，y 轴标注为 expression(x[2])，x、y 轴范围均为 c(0,1)，点形状为 16，点颜色为 blue，点大小为 4，轴标签大小为 2，轴大小为 2，纵横比为 1。

绘制 D 的散点图，其中 x 轴标注为 expression(x[1])，y 轴标注为 expression(x[2])，x、y 轴范围均为 c(0,1)，点形状为 16，点颜色为 blue，点大小为 4，轴标签大小为 2，轴大小为 2，纵横比为 1。继续调用 maxmin.augment() 函数序贯构造极大极小点，其中点数共 13 个，2 维，初始矩阵为 D，候选集为 CAND，返回的设计点横纵坐标矩阵命名为 D2。依据公式

$$
\psi(D) := \frac{1}{2}\min_{i\neq j}\|\boldsymbol{x}_i-\boldsymbol{x}_j\|
$$

计算 $\psi(D)$ 并打印。叠加第二次设计点散点图，其中大小为 4，线宽为 5，点型为 4，颜色为 red。将画布还原回 1x1。

```r

options(repr.plot.width=20,repr.plot.height=10)
par(mfrow=c(1,2))
p=2;n=7
set.seed(1)
library(SFDesign)
CAND=full.factorial(p=2,level=5)
D=maximin.augment(n=7,p=2,D.ini=matrix(c(.5,.5),nrow=1,ncol=p),candidate=CAND)
cat("r1:",min(dist(D))/2,"\n")
plot(D,xlim=c(0,1),ylim=c(0,1),cex=4,xlab=expression(x[1]),ylab=expression(x[2]),cex.lab=2,cex.axis=2,pch=16,col="blue",asp=1)
plot(D,xlim=c(0,1),ylim=c(0,1),cex=4,xlab=expression(x[1]),ylab=expression(x[2]),cex.lab=2,cex.axis=2,pch=16,col="blue",asp=1)
D2=maximin.augment(n=13,p=2,D.ini=D,candidate=CAND)
cat("r2:",min(dist(D2))/2,"\n")
points(D2[8:13,],cex=4,lwd=5,pch=4,col="red")
par(mfrow=c(1,1))

```

$$
\bigstar\;\diamond\;\bigstar\quad\boxed{\sigma_{\omega}\sigma}\quad\bigstar\;\diamond\;\bigstar
$$