$$
\bigstar\;\diamond\;\bigstar\quad\boxed{\sigma_{\omega}\sigma}\quad\bigstar\;\diamond\;\bigstar
$$

# 图 4.13

## 理论

小型极大极小设计（样本量与维度比值 $n_1/p$ 较小）不利于相关参数的估计，该现象是极大极小设计投影效果较差所造成的固有问题。图 4.13 展示了六维、64 次试验的极大极小设计样本点两两间距的直方图。由于 $64<2^6$，所有样本点都会被挤压至超立方体 $[0, 1]^6$ 的各个顶点处，最终仅存在少量互不相同的两两间距。该图同时给出了 64 次试验的最大投影（MaxPro）设计对应的样本两两间距分布。可以观察到最大投影设计的样本间距几乎呈连续分布，能够利用形状参数为 10.1 与 12.7 的缩放贝塔分布实现良好拟合，贝塔分布的形状参数由样本量 $n$ 与维度 $p$ 确定。尽管最大投影设计的设计初衷是保障预测效果，但该类设计的样本两两间距恰好天然具备贝塔分布特性，因此可以推断，最大投影设计在相关参数估计方面同样能取得优良效果。

## 实践

创建画布宽 10 英寸、高 10 英寸。令 p=6，n=64，设置种子为 1。导入 SFDesign 包，调用 maxproLHD() 函数先生成 n 个、p 维的初始极大极小拉丁超立方设计，取其 design 列再调用 maxpro.optim() 函数进行极大投影设计优化，其中选择多次随机生成初始 LHD，择优挑选最优初始设计，再开始梯度优化，取其 design 列作为最终的极大投影设计点，令其为 D1。调用 maxproLHD() 函数生成 n 个、p 维的初始极大极小拉丁超立方设计，取其 design 列再调用 maxpro.optim() 函数进行极大极小设计优化，取其 design 列作为最终的极大极小设计点，令其为 D2。分别计算 D1、D2 点集的两两距离，封装成普通向量分别令其为 d1、d2。导入 fitdistrplus 包，调用 fitdist() 函数对 d2 除以 sqrt(p)（归一化）的结果进行贝塔分布拟合，取其 estimate 列的列表正是贝塔分布的两个参数，分别令其为 shape1，shape2。绘制 d1 的直方图，纵轴显示概率密度，x 轴范围为 c(0, sqrt(p))（归一化），颜色为 25% 的 red，x 轴标签为“两两距离”，主标题为“距离分布”，标签大小、坐标轴大小、标题大小分别为 2、2、3。叠加 d2 的直方图，纵轴显示概率密度，颜色为 25% 的 blue，直方图分为 30 条，绘图方式为叠加。叠加贝塔分布的概率密度曲线，其中 x 取值范围为 c(0, sqrt(p))，形状参数分别为 x/sqrt(p)（归一化）、shape1、shape2，绘图方式为叠加，颜色为 3，线宽为 4。添加图例，其中位置在 (0,3)，标签分别为“极大极小设计”、“极大投影设计”、“Beta (10.1,12.7)”，颜色分别为 25% 的 red、25% 的 blue、3，线宽分别为 20、20、4，不显示边框，大小为 2。

```r

options(repr.plot.width=10,repr.plot.height=10)
p=6;n=64
set.seed(1)
library(SFDesign)
D1=maximin.optim(maximinLHD(n,p)$design,find.best.ini=TRUE)$design
D2=maxpro.optim(maxproLHD(n,p)$design)$design
d1=c(dist(D1))
d2=c(dist(D2))
library(fitdistrplus)
a=fitdist(d2/sqrt(p),"beta")
cat("shape1:",a$estimate[1],"\n")
cat("shape2:",a$estimate[2],"\n")
hist(d1,prob=TRUE,xlim=c(0,sqrt(p)),col=adjustcolor("red",0.25),xlab="两两距离",main="距离分布",cex.lab=2,cex.axis=2,cex.main=3)
hist(d2,prob=TRUE,col=rgb(0,0,1,1/4),breaks=30,add=TRUE)
curve(dbeta(x/sqrt(p),a$estimate[1],a$estimate[2])/sqrt(p),from=0,to=sqrt(p),add=TRUE,col=3,lwd=4)
legend(0,3,legend=c("极大极小设计","极大投影设计","Beta (10.1,12.7)"),col=c(adjustcolor("red",0.25),adjustcolor("blue",0.25),3),lwd=c(20,20,4),bty="n",cex=2)

```

$$
\bigstar\;\diamond\;\bigstar\quad\boxed{\sigma_{\omega}\sigma}\quad\bigstar\;\diamond\;\bigstar
$$